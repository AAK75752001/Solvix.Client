<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Solvix.Client.MVVM.ViewModels"
             xmlns:model="clr-namespace:Solvix.Client.Core.Models"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:converters="clr-namespace:Solvix.Client.Core.Converters"
             x:Class="Solvix.Client.MVVM.Views.ChatPage"
             x:DataType="vm:ChatPageViewModel"
             BackgroundColor="{DynamicResource PageBackgroundColor}"
             Title="{Binding CurrentChat.DisplayTitle}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:IsOwnMessageToAlignmentConverter x:Key="MessageAlignmentConverter"/>
            <converters:MessageStatusToIconConverter x:Key="StatusIconConverter"/>
            <converters:MessageStatusToOpacityConverter x:Key="StatusOpacityConverter"/>
            <toolkit:IntToBoolConverter x:Key="IntToBoolConverter"/>
            <toolkit:InvertedBoolConverter x:Key="InverseBoolConverter" />
            <converters:BoolToColorConverter x:Key="StatusColorConverter" TrueColor="{DynamicResource PrimaryColor}" FalseColor="{DynamicResource TertiaryTextColor}"/>

            <!-- استایل حباب پیام -->
            <Style x:Key="MessageBubbleBorderStyle" TargetType="Border">
                <Setter Property="Padding" Value="16,12"/>
                <Setter Property="Margin" Value="8,4"/>
                <Setter Property="MaximumWidthRequest" Value="{OnPlatform WinUI=450, Android=320, iOS=320}"/>
                <Setter Property="StrokeThickness" Value="0"/>
                <Setter Property="HorizontalOptions" Value="Start"/>
                <Setter Property="Margin" Value="8,4,80,4"/>
                <Setter Property="StrokeShape">
                    <RoundRectangle CornerRadius="18,18,18,4"/>
                </Setter>
                <Setter Property="BackgroundColor" Value="{DynamicResource ReceivedMessageBubbleColor}"/>
                <Setter Property="Shadow">
                    <Shadow Brush="{DynamicResource ShadowColor}" Offset="0,1" Radius="4" Opacity="0.1"/>
                </Setter>
            </Style>

            <!-- استایل متن پیام -->
            <Style x:Key="MessageTextStyle" TargetType="Label">
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="HorizontalOptions" Value="Start"/>
                <Setter Property="HorizontalTextAlignment" Value="Start"/>
                <Setter Property="TextColor" Value="{DynamicResource ReceivedMessageTextColor}"/>
                <Setter Property="LineBreakMode" Value="WordWrap"/>
                <Setter Property="LineHeight" Value="1.25"/>
                <Setter Property="FontFamily" Value="Vazirmatn"/>
            </Style>

            <!-- استایل زمان پیام -->
            <Style x:Key="MessageTimeStyle" TargetType="Label">
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="TextColor" Value="{DynamicResource TertiaryTextColor}"/>
                <Setter Property="Opacity" Value="0.8"/>
                <Setter Property="HorizontalOptions" Value="End"/>
                <Setter Property="VerticalOptions" Value="End"/>
                <Setter Property="Margin" Value="8,2,0,0"/>
                <Setter Property="FontFamily" Value="Vazirmatn"/>
            </Style>

            <!-- استایل آیکون وضعیت پیام -->
            <Style x:Key="MessageStatusIconStyle" TargetType="Label">
                <Setter Property="FontFamily" Value="MaterialIcons"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="Margin" Value="4,-1,0,0"/>
                <Setter Property="VerticalOptions" Value="End"/>
                <Setter Property="TextColor" Value="{Binding Status, Converter={StaticResource StatusColorConverter}}"/>
                <Setter Property="Opacity" Value="{Binding Status, Converter={StaticResource StatusOpacityConverter}}"/>
            </Style>

            <!-- استایل باکس ورودی پیام -->
            <Style x:Key="ChatInputBorderStyle" TargetType="Border">
                <Setter Property="Stroke" Value="{DynamicResource FrameBorderColor}"/>
                <Setter Property="StrokeThickness" Value="1"/>
                <Setter Property="Padding" Value="8,6,10,6"/>
                <Setter Property="BackgroundColor" Value="{DynamicResource CardBackgroundColor}"/>
                <Setter Property="StrokeShape">
                    <RoundRectangle CornerRadius="24"/>
                </Setter>
                <Setter Property="Shadow">
                    <Shadow Brush="{DynamicResource ShadowColor}" Offset="0,2" Radius="8" Opacity="0.15"/>
                </Setter>
            </Style>

            <!-- استایل فیلد ورودی پیام -->
            <Style x:Key="ChatInputEditorStyle" TargetType="Editor">
                <Setter Property="Placeholder" Value="پیام بنویسید..."/>
                <Setter Property="AutoSize" Value="TextChanges"/>
                <Setter Property="MaximumHeightRequest" Value="100"/>
                <Setter Property="VerticalOptions" Value="Center"/>
                <Setter Property="BackgroundColor" Value="Transparent"/>
                <Setter Property="TextColor" Value="{DynamicResource PrimaryTextColor}"/>
                <Setter Property="PlaceholderColor" Value="{DynamicResource TertiaryTextColor}"/>
                <Setter Property="FontSize" Value="15"/>
                <Setter Property="Margin" Value="8,0"/>
                <Setter Property="FontFamily" Value="Vazirmatn"/>
            </Style>

            <!-- استایل دکمه‌های اینپوت -->
            <Style x:Key="ChatInputButtonStyle" TargetType="Button">
                <Setter Property="WidthRequest" Value="40"/>
                <Setter Property="HeightRequest" Value="40"/>
                <Setter Property="CornerRadius" Value="20"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="BackgroundColor" Value="Transparent"/>
                <Setter Property="TextColor" Value="{DynamicResource SecondaryTextColor}"/>
                <Setter Property="FontFamily" Value="MaterialIcons"/>
                <Setter Property="FontSize" Value="24"/>
                <Setter Property="VerticalOptions" Value="Center"/>
                <Setter Property="VisualStateManager.VisualStateGroups">
                    <VisualStateGroupList>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal" />
                            <VisualState x:Name="Pressed">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="{DynamicResource FrameBorderColor}" />
                                    <Setter Property="TextColor" Value="{DynamicResource PrimaryColor}" />
                                    <Setter Property="Opacity" Value="0.8" />
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="Disabled">
                                <VisualState.Setters>
                                    <Setter Property="TextColor" Value="{DynamicResource FrameBorderColor}" />
                                    <Setter Property="Opacity" Value="0.5" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </Setter>
            </Style>

            <!-- استایل دکمه ارسال -->
            <Style x:Key="SendButtonStyle" TargetType="Button">
                <Setter Property="WidthRequest" Value="42"/>
                <Setter Property="HeightRequest" Value="42"/>
                <Setter Property="CornerRadius" Value="21"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="BackgroundColor" Value="{DynamicResource PrimaryColor}"/>
                <Setter Property="TextColor" Value="{DynamicResource InverseTextColor}"/>
                <Setter Property="FontFamily" Value="MaterialIcons"/>
                <Setter Property="Text" Value="&#xE163;"/>
                <!-- آیکن ارسال -->
                <Setter Property="FontSize" Value="22"/>
                <Setter Property="VerticalOptions" Value="Center"/>
                <Setter Property="HorizontalOptions" Value="Center"/>
                <Setter Property="VisualStateManager.VisualStateGroups">
                    <VisualStateGroupList>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal" />
                            <VisualState x:Name="Pressed">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="{DynamicResource ButtonHighlightColor}" />
                                    <Setter Property="Scale" Value="0.95" />
                                </VisualState.Setters>
                            </VisualState>
                            <VisualState x:Name="Disabled">
                                <VisualState.Setters>
                                    <Setter Property="BackgroundColor" Value="{DynamicResource TertiaryTextColor}" />
                                    <Setter Property="Opacity" Value="0.5" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </Setter>
            </Style>

            <!-- استایل جداکننده تاریخ -->
            <Style x:Key="DateSeparatorStyle" TargetType="Border">
                <Setter Property="BackgroundColor" Value="{DynamicResource CardBackgroundColor}"/>
                <Setter Property="Padding" Value="12,6"/>
                <Setter Property="Margin" Value="0,12"/>
                <Setter Property="StrokeShape">
                    <RoundRectangle CornerRadius="12"/>
                </Setter>
                <Setter Property="HorizontalOptions" Value="Center"/>
                <Setter Property="Shadow">
                    <Shadow Brush="{DynamicResource ShadowColor}" Offset="0,1" Radius="2" Opacity="0.1"/>
                </Setter>
            </Style>

            <!-- استایل متن تاریخ -->
            <Style x:Key="DateTextStyle" TargetType="Label">
                <Setter Property="TextColor" Value="{DynamicResource SecondaryTextColor}"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="FontAttributes" Value="Bold"/>
                <Setter Property="FontFamily" Value="Vazirmatn"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.ToolbarItems>
        <ToolbarItem IconImageSource="{FontImageSource FontFamily=MaterialIcons, Glyph=more_vert, Color={DynamicResource PrimaryTextColor}}"
                     Command="{Binding GoToChatSettingsCommand}"/>
    </ContentPage.ToolbarItems>

    <Grid RowDefinitions="*, Auto" ColumnDefinitions="*" BackgroundColor="{DynamicResource PageBackgroundColor}">
        <!-- نوار نام کاربر -->
        <Border Grid.Row="0" Grid.Column="0" VerticalOptions="Start" 
                BackgroundColor="{DynamicResource CardBackgroundColor}"
                Margin="0,0,0,4" Padding="0" HeightRequest="0">
            <Border.Shadow>
                <Shadow Brush="{DynamicResource ShadowColor}" Offset="0,2" Radius="4" Opacity="0.1"/>
            </Border.Shadow>
        </Border>

        <!-- نشانگر لودینگ -->
        <ActivityIndicator Grid.Row="0" Grid.Column="0"
                           IsRunning="{Binding IsLoadingMessages}"
                           IsVisible="{Binding IsLoadingMessages}"
                           HorizontalOptions="Center" VerticalOptions="Center"
                           Color="{DynamicResource PrimaryColor}"/>

        <!-- لیست پیام‌ها -->
        <CollectionView Grid.Row="0" Grid.Column="0"
                        x:Name="MessagesCollectionView"
                        ItemsSource="{Binding Messages}"
                        SelectionMode="None"
                        VerticalOptions="FillAndExpand"
                        Margin="8,4"
                        ItemsUpdatingScrollMode="KeepLastItemInView">
            <CollectionView.EmptyView>
                <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
                    <Label Text="پیامی وجود ندارد" 
                           TextColor="{DynamicResource TertiaryTextColor}" 
                           FontSize="16" 
                           HorizontalTextAlignment="Center"
                           FontFamily="Vazirmatn"/>
                    <Label Text="این گفتگو را با ارسال پیام شروع کنید" 
                           TextColor="{DynamicResource TertiaryTextColor}" 
                           FontSize="14" 
                           HorizontalTextAlignment="Center"
                           Margin="0,8,0,0"
                           FontFamily="Vazirmatn"/>
                </StackLayout>
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="model:MessageModel">
                    <Grid Margin="0,2">
                        <!-- حباب پیام -->
                        <Border Style="{StaticResource MessageBubbleBorderStyle}">
                            <Border.Triggers>
                                <DataTrigger TargetType="Border" Binding="{Binding IsOwnMessage}" Value="True">
                                    <Setter Property="HorizontalOptions" Value="End"/>
                                    <Setter Property="Margin" Value="80,4,8,4"/>
                                    <Setter Property="StrokeShape">
                                        <RoundRectangle CornerRadius="18,18,4,18"/>
                                    </Setter>
                                    <Setter Property="BackgroundColor" Value="{DynamicResource SentMessageBubbleColor}"/>
                                </DataTrigger>
                            </Border.Triggers>
                            <VerticalStackLayout Spacing="3">
                                <!-- متن پیام -->
                                <Label Text="{Binding Content}" Style="{StaticResource MessageTextStyle}">
                                    <Label.Triggers>
                                        <DataTrigger TargetType="Label" Binding="{Binding IsOwnMessage}" Value="True">
                                            <Setter Property="TextColor" Value="{DynamicResource SentMessageTextColor}"/>
                                        </DataTrigger>
                                    </Label.Triggers>
                                </Label>

                                <!-- زمان و وضعیت پیام -->
                                <HorizontalStackLayout HorizontalOptions="End" Spacing="4">
                                    <Label Text="{Binding LocalSentAt, StringFormat='{0:HH:mm}'}" Style="{StaticResource MessageTimeStyle}"/>
                                    <Label IsVisible="{Binding IsOwnMessage}" 
                                           Text="{Binding Status, Converter={StaticResource StatusIconConverter}}" 
                                           Style="{StaticResource MessageStatusIconStyle}"/>
                                </HorizontalStackLayout>
                            </VerticalStackLayout>
                        </Border>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            <CollectionView.Header>
                <Grid Padding="0,8" IsVisible="{Binding IsLoadingMessages}">
                    <ActivityIndicator IsRunning="{Binding IsLoadingMessages}" 
                                       IsVisible="{Binding IsLoadingMessages}"
                                       Color="{DynamicResource PrimaryColor}"
                                       HorizontalOptions="Center"
                                       HeightRequest="40"
                                       WidthRequest="40"/>
                </Grid>
            </CollectionView.Header>
            <CollectionView.Footer>
                <BoxView HeightRequest="8"/>
            </CollectionView.Footer>
        </CollectionView>

        <!-- نوار نمایشگر درحال تایپ (برای آینده) -->
        <Label Grid.Row="0" Grid.Column="0" 
               Text="درحال تایپ..." 
               TextColor="{DynamicResource TertiaryTextColor}"
               FontSize="13" 
               Margin="16,0,0,8" 
               VerticalOptions="End"
               IsVisible="False"
               FontFamily="Vazirmatn"/>

        <!-- بخش ورودی پیام -->
        <Border Grid.Row="1" Grid.Column="0" 
                Style="{StaticResource ChatInputBorderStyle}"
                Margin="12,8,12,12">
            <Grid ColumnDefinitions="Auto,*,Auto,Auto" ColumnSpacing="4">
                <!-- دکمه اتصال فایل -->
                <Button Grid.Column="0" 
                        Text="&#xE226;" 
                        Command="{Binding AttachFileCommand}" 
                        Style="{StaticResource ChatInputButtonStyle}"
                        Margin="2,0,2,0"/>

                <!-- فیلد ورودی پیام -->
                <Editor Grid.Column="1" 
                        Text="{Binding NewMessageText, Mode=TwoWay}" 
                        Style="{StaticResource ChatInputEditorStyle}" 
                        IsEnabled="{Binding IsSendingMessage, Converter={toolkit:InvertedBoolConverter}}"/>

                <!-- دکمه ایموجی -->
                <Button Grid.Column="2" 
                        Text="&#xE7F2;" 
                        Command="{Binding EmojiCommand}" 
                        Style="{StaticResource ChatInputButtonStyle}"
                        Margin="2,0,4,0"/>

                <!-- دکمه ارسال -->
                <Button Grid.Column="3" 
                        Command="{Binding SendMessageCommand}" 
                        Style="{StaticResource SendButtonStyle}">
                    <Button.Triggers>
                        <DataTrigger TargetType="Button" 
                                     Binding="{Binding NewMessageText.Length, Converter={StaticResource IntToBoolConverter}}" 
                                     Value="False">
                            <Setter Property="IsEnabled" Value="False"/>
                            <Setter Property="Opacity" Value="0.5"/>
                        </DataTrigger>
                        <DataTrigger TargetType="Button" 
                                     Binding="{Binding IsSendingMessage}" 
                                     Value="True">
                            <Setter Property="IsEnabled" Value="False"/>
                            <Setter Property="Opacity" Value="0.5"/>
                        </DataTrigger>
                    </Button.Triggers>
                </Button>
            </Grid>
        </Border>

    </Grid>
</ContentPage>